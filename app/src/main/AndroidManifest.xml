<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="vivaladev.com.dirtyclocky">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <service
            android:name=".alarmcontrol.service.AlarmService"
            android:process=":alarmService"
            />
            <!-- android:exported="true" android:process=":alarmService" android:stopWithTask="false" -->
         <!--<receiver android:name=".alarmcontrol.receive.AlarmReceiver" android:process=":remote"/>-->
         <!--<receiver android:process=":remote" android:name=".alarmcontrol.receive.AlarmReceiver"/>
        <receiver android:name=".alarmcontrol.receive.AlarmReceiver">
            <intent-filter>
                <action android:name="ilku.ru.alarmclock.receive.ALARM" />
            </intent-filter>
        </receiver>-->

        <activity android:name=".ui.activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activities.AlarmEditActivity" />
        <activity android:name=".ui.activities.SoundProcessingActivity" />
        <activity android:name=".ui.activities.AlarmClockActivity"/>
    </application>

</manifest>